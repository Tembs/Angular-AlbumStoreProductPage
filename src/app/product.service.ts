import { Injectable } from '@angular/core';
import { Http, Response} from '@angular/http';
@Injectable()
export class ProductService {
	private _albunUrl = '../assets/album.json';
  constructor(private _http: Http) { }
private class _albumUrl 
}
